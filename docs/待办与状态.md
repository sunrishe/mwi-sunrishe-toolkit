# 待办与状态

更新日期：2026-08-02

本文只记录当前待办、验收入口、版本和交付状态。长期业务规则见 `docs/业务功能要求.md`；工程结构见 `docs/工程结构与开发规范.md`。

## 当前状态：2.7.22 输入与视口兼容修复

### 已完成

- Rollup 单文件构建已接入：正式包输出 `dist/mst.script.user.js`，开发包输出 `dist/mst.script.dev.user.js`。
- prod 构建将内嵌 CSS 输出为每条规则一行的紧凑格式，不对 JS 做 esbuild 压缩或语法收紧；dev/watch 构建保持调试可读。
- `yarn watch` 已接入，源码、油猴头部或构建配置变化后自动重建开发包。
- dev/prod 差异通过 `src/common/build-flags.js` 和 Rollup replace 控制；语言切换按钮只在开发包显示。
- raw CSS 插件已接入，全局 CSS 位于 `src/common/styles/`，房屋模块 CSS 位于模块目录，最终全部打进单个脚本。
- 角色名片样式已迁移到模块独立 CSS，动态尺寸由 JS 常量通过 `--mst-card-*` CSS Variables 提供。
- ESLint、Prettier、Husky 和 lint-staged 已接入工程流程。
- EditorConfig、Prettier、Git 属性及 VS Code 工作区配置已统一为 UTF-8、LF 和 2 空格缩进，不同编辑器使用相同格式规则。
- 包管理已统一切换为 Yarn Classic `1.22.22`，仅保留 `yarn.lock`。
- i18n 文案已集中到 `src/common/messages.js`，测试校验中英文完整性和 `i18n.t()` 引用。
- 基础数据缓存优先通过游戏官方 `window.localStorageUtil.getInitClientData()` 读取，并保留 `document-start` 阶段的缓存解压兜底。
- 源码已收敛为 `src/app/`、`src/common/`、`src/modules/` 三层。
- 业务模块按用户功能聚合，默认以 `index.js` 为主体；仅计算、Worker、导出、稳定数据等真实边界单独成文件。
- 旧 `core/data/services/ui/features/i18n/styles` 碎片目录、旧应用入口和一次性归并脚本已删除。
- 测试已全部切换到新目录，不再依赖旧文件假通过。
- 工具箱、用户名片、技能升级、房屋升级、战斗升级、装备提升、地下城收益和 EDS 跨站能力均已接入新模块结构。
- 用户名片顶部聊天图标 class 拼接已恢复与旧脚本一致，避免图标尺寸失控撑高名片 header。
- 地下城收益计算器在官方语言资源尚未就绪时，会使用内置官方地下城中英文名称兜底。
- 地下城收益计算器复选框已改为接近名片双列切换的紧凑按钮外观。
- 地下城收益操作区保留地下城、难度、单次耗时、每日药品/饮料成本、工匠茶、暴饮之囊强化等级和自定义模式；每日固定按 24 小时计算。
- 地下城首次打开默认选择官方排序的第一个地图、T0 和 30 分钟单次耗时。
- 地下城操作区控件按合理最小宽度自动调整等宽列数，最后一行保持相同列宽并允许留空，不再拉伸填满整行。
- 四个地下城的普通宝箱固定为每车 `1.295`，T1/T2 精炼宝箱分别为 `1.295 × 0.33` 和 `1.295`；队伍人数和战斗掉落 Buff 不参与计算。
- 每日轮次、单车/每日宝箱和门票均按两位小数计算，显示时去掉尾随 `0`；门票数量等于普通宝箱期望数量。
- 地下城已移除成本方式切换，顶部单层分组表头同时展示制作钥匙与购买钥匙；材料成本分区只显示买入方向，预期产出分区只显示卖出方向。
- 地下城新增自定义模式，可选择制作/购买钥匙、左买/右买及左卖/右卖；左侧两列保留所选钥匙来源区间，右侧两列合并显示自定义组合结果。
- 地下城中文价格方向统一简称为“左买 / 右买 / 左卖 / 右卖”，英文 Ask/Bid 买卖文案保持不变。
- 地下城表单和结果表使用独立渲染边界，切换钥匙来源、购入方式或卖出方式后，列头和自定义金额立即同步更新。
- 地下城下拉选项保持单根节点模板，连续切换自定义模式或语言时不会再因 `uhtml` Fragment 锚点失效而报错。
- 地下城工匠茶和自定义模式复选框与同排下拉框保持底边水平对齐。
- 地下城弹窗最大宽度为 `38rem`，结果表最小宽度为 `35rem`；默认模式按 `27% / 13% / 15% × 4` 分配，自定义模式按 `27% / 13% / 20% × 3` 分配有效列，自定义结果不再占用双列宽度。操作区在桌面端和移动端均按可用宽度自适应排列。
- 地下城结果列表明确分为上方的材料成本与下方的预期产出；按输入条件计算期望宝箱，再按官方掉落表递归展开期望物品，重复物品合并并继续展开嵌套宝箱。
- 门票和宝箱钥匙只展示单位和每日成本，不展示每车成本或材料消耗格子；工匠茶与暴饮之囊只影响制作成本。
- 每日药品/饮料成本可按 `M` 输入，并从制作/购买两套每日及每车期望收益中分摊扣除。
- 工匠茶默认勾选；暴饮之囊使用与名片双列切换一致的独立无文本方形复选控件，在当前装备、背包和仓库的全部持有物品中选择最高强化等级，拥有时自动勾选，没有时不勾选；等级选择只由该复选框控制。
- 每日期望掉落按 `bid/ask` 两种卖出总价计价，统一扣除 2% 市场税，再分别减去制作/购买的门票与宝箱钥匙成本；同时展示每车期望收益。
- 普通与精炼宝箱税后收益均显示单个宝箱的净收益，数量列保留每日宝箱期望；预期产出区先显示每车期望收益，再显示每日期望收益，每日期望收益按单箱收益乘数量汇总后只额外扣除药品/饮料成本。
- 每日轮次、门票、普通宝箱、可选精炼宝箱和市场数据时间摘要保持单行四等分或五等分，不再换行或保留空格；每日及每车期望收益与宝箱税后收益使用相同样式。
- 市场某一侧价格为 `-1` 时会按有效价格回退，不再把仍可估值的宝箱产出直接按 `0` 计算。
- 技能升级列表为纵向滚动条预留 `1rem` 宽度，桌面端默认打开和列表纵向滚动时均不再出现无意义的横向滚动条。
- 装备提升弹窗按中英文最长内容由 `46rem` 收紧为 `34.5rem`，属性表最小宽度由 `34rem` 收紧为 `32.5rem`，并重新分配四列宽度。
- 技能升级中文市场列头由“最佳出售价 / 总价”和“最佳收购价 / 总价”统一简称为“左一 / 总价”和“右一 / 总价”，英文保持不变。
- 技能升级、房屋升级、战斗升级、装备提升和地下城收益弹窗的标题提示说明已按当前逻辑统一调整；房屋升级弹窗新增标题问号说明入口。
- 已修正 House Room 中文术语：用户界面泛称按游戏官方译名统一为“房屋”；技能升级市场列头继续使用“左一 / 总价”和“右一 / 总价”。
- 战斗升级和技能升级的等级输入在 Firefox、Chrome、Edge 和 Safari 下统一隐藏原生数字加减箭头，避免与常用等级下拉控件冲突。
- 技能升级连续添加多个技能后，弹窗会随内容变化重新限制在可视区域内；技能选择浮层改为固定在实际视口内显示。
- 工具箱下拉菜单改为按 `visualViewport` 动态定位，在手机浏览器地址栏显示/收起导致可视高度变化时仍保持在屏幕内。

### 当前模块

| 模块            | 主体文件                                    | 独立边界                         |
| --------------- | ------------------------------------------- | -------------------------------- |
| 技能升级        | `src/modules/ability-upgrade/index.js`      | 无                               |
| 角色名片        | `src/modules/character-card/index.js`       | 数据、渲染、导出、样式、战力计算 |
| 战斗升级        | `src/modules/combat-upgrade/index.js`       | 练级计划计算                     |
| 地下城收益      | `src/modules/dungeon-profit/index.js`       | 收益计算                         |
| EDS / Milkonomy | `src/modules/eds-milkonomy/index.js`        | 配装转换                         |
| 装备提升        | `src/modules/equipment-comparison/index.js` | 比较、模拟、预设、Worker         |
| 房屋升级        | `src/modules/house-calculator/index.js`     | 材料计算、静态 CSS               |
| 工具箱菜单      | `src/modules/toolkit-menu/index.js`         | 无                               |

### 工具箱动作顺序

1. 用户名片
2. 技能升级
3. 房屋升级
4. 战斗升级
5. 装备提升
6. 地下城收益
7. 切换角色

## 后续待办

1. 在真实游戏页面完成主要 UI 手工回归，覆盖工具箱、弹窗、窄屏、语言切换和重复插入。
2. 后续新增功能继续按用户功能聚合，避免重新引入按职责机械细拆的目录。

## 验证入口

- `yarn lint`：检查 `src`、`tests` 和构建配置。
- `yarn format:check`：检查 JS、CSS、配置和受管文档格式。
- `yarn test`：运行 Node 回归测试。
- `yarn build:prepare`：依次执行 `lint:fix` 和 `format`。
- `yarn build:dev`：生成开发油猴脚本，不自动执行源码修复。
- `yarn build`：先执行源码修复，再生成正式油猴脚本。
- `yarn run check`：执行一次源码修复、测试、dev 打包和 prod 打包，避免重复修复。

### 地下城 Playwright MCP 回归清单

- 只能使用 `mcp__playwright__*`，不得用 `playwright-cli`、Browser plugin 或 Node REPL 代替。
- 核对基础操作区包含地下城、难度、单次耗时、每日药品/饮料成本、工匠茶、暴饮之囊和自定义模式；工匠茶默认勾选，暴饮之囊按角色装备、背包和仓库内的最高强化等级自动设置。页面不得出现旧成本方式、队伍人数、角色 Buff、Combat Drop Quantity、Combat Drop Rate、统计周期和市场税开关。
- 14 分钟下分别验证 T0/T1/T2：每日轮次 `102.86`、普通宝箱和门票 `133.2`，精炼宝箱依次为不展示、`43.96`、`133.2`。
- 核对顶部单层表头显示“制作钥匙 / 购买钥匙”；材料成本分区行只显示“左买 / 右买”，预期产出分区行只显示“右卖 / 左卖”，英文对应 `Ask Buy / Bid Buy` 和 `Bid Sell / Ask Sell`，两种语言不得混排。
- 勾选自定义模式后，核对钥匙来源、钥匙材料/成品购买方式和货物出售方式按来源动态显示；分别验证制作/购买钥匙及四种买卖档位组合会更新右侧合并结果，且左侧始终保留所选来源的两列区间。
- 启用工匠茶后切换暴饮之囊复选框，确认等级选择同步启用/禁用，且只有制作钥匙的单位与每日成本更新。
- 输入每日药品/饮料成本，确认成本行动态出现、两套每日总成本增加相同金额、每车收益按每日轮次分摊扣除；清空后成本行消失。
- 核对门票/钥匙只展示单位与每日成本，不存在每车成本和材料消耗格子；预期产出区只存在普通宝箱、非 T0 的精炼宝箱、每车期望收益和每日期望收益。
- 核对列表分区标题为上方“材料成本”和下方“预期产出”。
- 确认普通/精炼宝箱税后收益为单箱值，每日收益等于各类单箱收益乘对应每日数量后再减药品/饮料成本，每车收益等于每日收益除以每日轮次。
- 核对包含市场数据时间的 T0 四个摘要格和 T1/T2 五个摘要格始终保持同一行，不存在空白占位。
- 核对相同参数在重绘和重新打开计算器后结果完全一致，不存在随机波动。
- 至少验证 1280px 与 390px 视口下操作区自动调整列数、最后一行不拉伸、摘要保持单行、表格只在自身容器横向滚动，同时核对文字可见性、业务错误和控制台错误。
- 优先验证登录后的实际游戏页面；只能使用本地运行时页面时，记录实际引用的源码、页面 URL、未覆盖范围，且不得写成实际目标页面已通过。

## 2.7.20 验收记录（2026-08-02）

- 文档规则：`docs/usage.md` 更新日志已合并临时版本记录，只保留当前 `v2.7.20`，并从用户角度概括从 EDS 升级到 MST 当前版本后的主要能力。
- 文案范围：保留 EDS 与 MST 的版本规划说明；当前版本日志覆盖配装同步、角色/队伍/配装名片、升级计算、装备提升计算器、地下城收益、市场伴侣增强和整体体验优化。
- 自动检查：`yarn format:check` 通过，全仓受管文件符合 Prettier 格式。

## 2.7.19 验收记录（2026-08-02）

- 文案规则：`userscript-header.txt` 中文描述同步使用“装备提升计算器”，与工具箱入口、弹窗标题和使用说明一致；英文 `equipment comparison` 保持不变。
- 自动检查：`yarn run check` 通过，覆盖全量 ESLint、全仓 Prettier、全量 `114/114` 测试、`yarn build:dev` 和 `yarn build`；构建产物版本已更新为 `2.7.19` 和 `2.7.19-dev.<timestamp>`。

## 2.7.18 验收记录（2026-08-02）

- 文档核对：README、工程规范、业务要求、使用说明、状态文档和 `docs/analysis/` 已按现有目录、构建配置、工具箱顺序、地下城表格方向和装备提升候选过滤逻辑重新核对。
- 同步修正：README 的 `dist/` 忽略说明改为开发包忽略；英文使用说明的市场价格术语同步为 `Best Ask Price / Best Bid Price`；地下城 Playwright MCP 回归清单改为成本区“左买 / 右买”、产出区“右卖 / 左卖”；装备提升说明和弹窗帮助明确排除护符、Trinket 和生活工具，不再把项链、耳环、戒指混称为被排除的饰品。
- 自动检查：`yarn run check` 通过，覆盖全量 ESLint、全仓 Prettier、全量 `114/114` 测试、`yarn build:dev` 和 `yarn build`；构建产物版本已更新为 `2.7.18` 和 `2.7.18-dev.<timestamp>`。

## 2.7.17 验收记录（2026-08-02）

- 文案规则：中文工具箱入口、弹窗标题和使用文档统一为“装备提升计算器”；英文 `Equipment Comparison` 保持不变。
- 自动检查：`yarn run check` 通过，覆盖全量 ESLint、全仓 Prettier、全量 `114/114` 测试、`yarn build:dev` 和 `yarn build`；构建产物版本已更新为 `2.7.17` 和 `2.7.17-dev.<timestamp>`。

## 2.7.16 验收记录（2026-08-02）

- 文案规则：House Room 用户界面泛称按游戏官方译名恢复为“房屋”；`Best Ask Price / Best Bid Price` 中文继续对应“左一 / 右一”。
- 测试规则：新增回归测试锁定 House Room 中文必须为“房屋”，并继续锁定技能升级市场列头“左一 / 总价”“右一 / 总价”和地下城价格方向“左买 / 右买 / 左卖 / 右卖”。
- 自动检查：全量 ESLint、全量 `114/114` 测试、`yarn build:dev` 和 `yarn build` 通过；构建产物版本已更新为 `2.7.16` 和 `2.7.16-dev.<timestamp>`。
- 完整检查：`yarn run check` 已执行，但停在仓库既有 Prettier 格式检查范围；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开房屋升级材料计算器和技能升级计算器。房屋升级说明显示“勾选需要升级的房屋 / 每个房屋按官方升级配置 / 房屋数据、库存和市场价格”，未出现错误的“房间”表述；技能升级中文市场列头显示“左一 / 总价”和“右一 / 总价”，未出现旧的“最佳出售价 / 最佳收购价”。控制台唯一错误为 Facebook Pixel `fbevents.js` 被浏览器拦截，与 MST 无关。

## 2.7.15 验收记录（2026-08-02）

- 文案规则：参考游戏官方中文资源和本地游戏站资源，单个 House Room 相关中文统一为“房间”；`Best Ask Price / Best Bid Price` 中文继续对应“左一 / 右一”。
- 自动检查：全量 ESLint、本轮涉及文件 Prettier 检查、全量 `114/114` 测试、`yarn build:dev` 和 `yarn build` 通过；构建产物版本已更新为 `2.7.15` 和 `2.7.15-dev.<timestamp>`。
- 完整检查：`yarn run check` 已执行，但停在仓库既有的 22 个非本轮 Prettier 文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开房屋升级材料计算器和技能升级计算器。房屋升级说明显示“勾选需要升级的房间 / 每个房间按官方升级配置”，未出现旧的“每个房屋”说明；技能升级中文市场列头仍为“左一 / 总价”和“右一 / 总价”，未出现旧的“最佳出售价 / 最佳收购价”。控制台唯一错误为 Facebook Pixel `fbevents.js` 被浏览器拦截，与 MST 无关。

## 2.7.14 验收记录（2026-08-02）

- 文案规则：技能升级、房屋升级、战斗升级、装备提升和地下城收益的弹窗说明均按当前输入、计算、限制和市场伴侣逻辑重新整理；房屋升级弹窗补齐与其他计算器一致的标题问号入口。
- 自动检查：全量 ESLint、本轮涉及文件 Prettier 检查、全量 `113/113` 测试、`yarn build:dev` 和 `yarn build` 通过；构建产物版本已更新为 `2.7.14` 和 `2.7.14-dev.<timestamp>`。
- 完整检查：`yarn run check` 已执行，但停在仓库既有的 22 个非本轮 Prettier 文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开房屋升级材料计算器。中文弹窗标题右侧显示“查看房屋升级说明”入口，点击后显示中文提示内容；切换英文后标题、入口和提示内容同步为英文，再切回中文。控制台唯一错误为 Facebook Pixel `fbevents.js` 被浏览器拦截，与 MST 无关。

## 2.7.13 验收记录（2026-08-02）

- 内容测量：中文最长项目文本为 `140px`，含单元格内边距约需 `152px`；将项目列由 `35%` 收紧到 `27%` 后，桌面实际宽度由 `204.41px` 降为 `157.67px`，仍保持中文单行。
- 候选尺寸验证：桌面默认模式四个金额列由 `75.91px` 扩为 `87.59px`，自定义模式三个有效金额列为约 `116.8px`；英文长项目自然换行，1280px 和 390px 下单元格、弹窗及页面均无溢出。
- 自动检查：地下城专项 `31/31`、全量 `113/113` 测试、本轮涉及文件的 ESLint 与 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 重新加载 `https://www.milkywayidle.com/game?characterId=427012`。1280px 中文默认模式下表格为 `584px`，项目列为 `157.67px`，数量列为 `75.91px`，金额列为 `87.59px`；英文自定义模式下三个有效金额列约为 `116.8px`，全部单元格、弹窗和页面均无横向溢出。
- Playwright MCP 移动端验证：390px 英文自定义模式下表格保持 `392px` 并只在自身容器横向滚动，项目列约 `105.83px`，全部单元格、弹窗和页面均无内容或页面级溢出；中英文与自定义模式往返切换未产生 MST 业务错误，控制台仅有 Facebook Pixel 被浏览器拦截。

## 2.7.12 验收记录（2026-08-02）

- 文案规则：技能升级中文市场列头显示“左一 / 总价”和“右一 / 总价”；英文继续显示 `Best Ask Price / Total` 和 `Best Bid Price / Total`。
- 自动检查：国际化专项 `30/30`、全量 `113/113` 测试、涉及文件 ESLint 与 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- Playwright MCP 真实页面验证：在 1280px 中文界面确认两列显示“左一 / 总价”和“右一 / 总价”，切换英文后恢复 `Best Ask Price / Total` 和 `Best Bid Price / Total`；两种语言下表格及页面横向溢出均为 `0`。

## 2.7.11 验收记录（2026-08-02）

- 内容测量：装备提升中文四列最长内容（含单元格内边距）约需 `98 / 76 / 71 / 74px`；英文约需 `143 / 139 / 162 / 80px`。据此将弹窗和表格收紧，并按 `27.5% / 26.5% / 30.75% / 15.25%` 分配列宽。
- 自动检查：宽度专项 `27/27`、全量 `113/113` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- 完整检查：`yarn run check` 仍停在仓库既有的 23 个非本轮 Prettier 文件，本次未批量格式化无关文件；其余检查已按上一项分别通过。
- Playwright MCP 桌面验证：在真实游戏页的 1280px 中英文界面中，技能升级空列表和职业预设列表的横向溢出均为 `0`，单元格内容溢出为 `0`；强制出现纵向滚动条时 `clientWidth` 与 `scrollWidth` 均为 `782px`。装备提升弹窗和属性表分别为 `552px / 528px`，四列约为 `145.19 / 139.91 / 162.36 / 80.55px`，中英文表头保持单行且无横向溢出。
- Playwright MCP 移动端验证：390px 下技能升级和装备提升弹窗、配置区及页面横向溢出均为 `0`；技能列表只在自身容器保留必要的横向滚动，装备属性表全部单元格均无内容溢出。当前导航周期控制台仅有 Kongregate 资源连接关闭和 Facebook Pixel 被浏览器拦截两个站外错误，没有 MST 业务错误。

## 2.7.10 验收记录（2026-08-02）

- 内容测量：中文最长项目文案实际单行宽度约为 `154.4px`，原项目列为 `252.81px`。将项目列收紧到 `35%` 后，桌面实际宽度为 `204.41px`；中文保持单行，最长英文项目自然换成两行且无溢出。
- 自动检查：地下城专项 `31/31`、全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- 完整检查：`yarn run check` 仍停在仓库既有的 24 个非本轮 Prettier 文件，本次未批量格式化无关文件；其余检查已按上一项分别通过。
- Playwright MCP 桌面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 的 1280px 真实页面中，弹窗和表格由 `656px / 632px` 缩小到 `608px / 584px`；默认列宽约为 `204.41 / 75.91 × 5px`，自定义有效列宽约为 `204.41 / 75.91 / 101.22 / 101.22 / 101.25px`。中文 T0、英文 T2、默认和自定义模式均无单元格、弹窗或页面溢出。
- Playwright MCP 移动端验证：390px 下表格宽度由约 `425.59px` 缩小到 `392px`，表格容器横向滚动量由约 `62px` 降到 `28px`；默认和自定义模式的弹窗及页面横向溢出均为 `0`。页面级错误监听结果为 `0`，控制台仅有 Facebook Pixel 被浏览器拦截。

## 2.7.9 验收记录（2026-08-02）

- 内容测量：中文最大内容宽度为项目 `157px`、数量 `52px`、金额 `73px`；英文最大内容宽度为项目 `246px`、数量 `70px`、金额 `73px`。据此缩小默认列宽，并将自定义结果从双列宽度改为单个有效金额列。
- 自动检查：地下城专项 `31/31`、全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- 完整检查：`yarn run check` 仍停在仓库既有的 24 个非本轮 Prettier 文件，本次未批量格式化无关文件；其余检查已按上一项分别通过。
- Playwright MCP 桌面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 的 1280px 真实页面中，弹窗和表格分别为 `656px / 632px`；默认列宽约为 `252.81 / 75.83 × 5px`，自定义有效列宽约为 `252.81 / 75.83 / 101.11 / 101.11 / 101.14px`，末尾占位列为 `0px`，所有单元格均无溢出。
- Playwright MCP 交互验证：连续切换自定义模式 7 次，并切换钥匙来源、购买档位、出售档位和中英文，列头、数值和宽度均同步更新；修复验证使用的页面级错误监听结果为 `0`。
- Playwright MCP 移动端验证：390px 下弹窗和页面横向溢出均为 `0`，表格只在自身容器内滚动；默认和自定义模式均无单元格文字溢出。控制台仅有 Facebook Pixel 被浏览器拦截，与 MST 无关。

## 2.7.8 验收记录（2026-08-02）

- 宽度分配：按中英文项目名、数量和金额格式收紧结果表列宽，桌面弹窗最大宽度由 `60rem` 缩小为 `48rem`。
- 自动检查：地下城专项 `31/31`、全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- Playwright MCP 真实页面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 中，1280px 英文界面弹窗和表格实测为 `768px / 744px`，项目、数量和四个金额列约为 `245.52 / 74.39 / 106.02 × 4px`，最长项目名保持单行且没有单元格溢出；390px 下表格仅在自身容器横向滚动，最长英文项目正常换行，页面和弹窗横向溢出均为 `0`。控制台仅有 Facebook Pixel 被浏览器拦截。

## 2.7.7 验收记录（2026-08-02）

- 展示顺序：预期产出区将每车期望收益移动到每日期望收益之前，计算口径不变。
- 自动检查：地下城专项 `31/31`、全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- Playwright MCP 真实页面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 中确认预期产出依次显示“单个普通宝箱税后收益 → 每车期望收益 → 每日期望收益”；页面和弹窗横向溢出均为 `0`，控制台仅有 Facebook Pixel 被浏览器拦截。

## 2.7.6 验收记录（2026-08-02）

- 表头与宽度：材料成本分区仅显示买入方向，预期产出分区仅显示卖出方向；结果表重新分配列宽并将弹窗最大宽度缩小至 `60rem`。
- 自动检查：地下城专项 `31/31`、全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。`yarn run check` 已执行，但全仓 Prettier 仍被 24 个本轮未修改的既有文件拦截。
- Playwright MCP 真实页面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 中，1280px 弹窗宽度为 `960px`，默认模式项目、数量和四个金额列实测约为 `224.63 / 93.59 / 154.44 × 4px`；390px 下页面和弹窗横向溢出均为 `0`，仅结果表容器内部滚动。默认模式下材料成本仅显示“左买 / 右买”，预期产出仅显示“右卖 / 左卖”；自定义切换为购买钥匙、右买和右卖后，两处分区列头与自定义金额同步更新。全新加载后连续切换各自定义控件未捕获业务异常，控制台仅有 Facebook Pixel 被浏览器拦截。

## 2.7.5 验收记录（2026-08-02）

- 布局与表头：操作区改为 `auto-fill` 自适应等宽网格，最后一行不拉伸；结果表移除第二层固定表头，材料成本和预期产出分区行分别重复显示价格方向。
- 自动检查：地下城专项 `31/31`、全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- Playwright MCP 真实页面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 中，1280px 默认模式为五列，最后一行两个控件保持约 `207.4px` 标准列宽；390px 为两列，最后一行单个控件保持约 `180.3px` 半行宽。两种视口下页面、弹窗和操作区横向溢出均为 `0`。结果表顶部只有一行分组表头，材料成本和预期产出分区均显示完整价格方向，自定义合并列头正常。控制台仅有 Facebook Pixel 被浏览器拦截。

## 2.7.4 验收记录（2026-08-02）

- 响应式布局：地下城操作区改为可换行 Flex 布局，控件使用自适应基础宽度并均分每行剩余空间，移除固定四列及移动端固定两列规则。
- 自动检查：地下城专项 `31/31`、全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- Playwright MCP 真实页面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 中，1280px 下默认模式自动排列为 `5+2`，自定义模式为 `5+5`；390px 下分别为 `3+3+1` 和 `3+3+3+1`。每行左右空隙均为 `0`，最后一行自动拉伸占满；页面、弹窗、操作区及各字段横向溢出均为 `0`，中文长标签无重叠。控制台仅有 Facebook Pixel 和 Kongregate 第三方资源加载失败。

## 2.7.3 验收记录（2026-08-02）

- 中文文案：地下城价格方向统一简称为“左买 / 右买 / 左卖 / 右卖”，英文 `Ask Buy / Bid Buy / Ask Sell / Bid Sell` 保持不变。
- 自动检查：全量 `112/112` 测试、全量 ESLint、本轮修改文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。`yarn run check` 在全仓 Prettier 阶段被 24 个与本次无关的既有格式问题拦截，未改动这些文件。
- Playwright MCP 真实页面验证：在 `https://www.milkywayidle.com/game?characterId=427012` 中，中文自定义下拉框显示“左买 / 右买”和“左卖 / 右卖”，默认及自定义列头同步使用简称；英文下拉框与列头仍显示 `Ask Buy / Bid Buy / Ask Sell / Bid Sell`。反复切换自定义模式未产生 MST 控制台错误，仅有 Facebook Pixel 和 Kongregate 第三方资源加载失败。

## 2.7.2 验收记录（2026-08-02）

- 自动检查：地下城专项 31 项测试、全量 112 项测试、全量 ESLint、本轮 5 个源码与文档文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。
- 对齐修复：移除地下城复选框字段原有的 `0.3rem` 底部内边距，继续使用字段底部对齐，不影响暴饮之囊内部的独立复选框。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 重新加载 `https://www.milkywayidle.com/game?characterId=427012`。1280px 下工匠茶、暴饮之囊、自定义模式和钥匙来源控件的顶部/底部均为 `241.75 / 271.75px`；390px 两列布局下每组同排控件的顶部、底部和高度也完全一致，操作区横向溢出为 `0`。控制台只剩 Facebook Pixel 被浏览器拦截，与 MST 无关。

## 2.7.1 验收记录（2026-08-02）

- 自动检查：地下城专项 31 项测试、全量 112 项测试、全量 ESLint、本轮 5 个源码与文档文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。`yarn run check` 已执行，但全仓 Prettier 仍被 24 个本轮未修改的既有文件拦截。
- 交互修复：地下城表单和结果表改为独立的 `uhtml` 渲染边界，避免切换钥匙来源、购入方式或卖出方式后，结果表继续复用已经脱离 DOM 的旧节点。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 重新加载 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.7.1` 开发构建。制作/购买钥匙下各自验证四种买卖档位组合，列头依次正确显示四种方向，门票、开箱钥匙、单箱收益和每日收益均随对应参数即时更新。
- 真实市场数据核对：制作钥匙的四组每日收益为 `39.68M / 32.61M / 44.93M / 37.86M`，购买钥匙为 `34.8M / 27.73M / 42.26M / 35.19M`；八组页面异常数组均为空。控制台只剩 Facebook Pixel 被浏览器拦截，与 MST 无关。

## 2.7.0 验收记录（2026-08-02）

- 自动检查：地下城专项 30 项测试、全量 111 项测试、全量 ESLint、本轮 10 个涉及文件的 Prettier 检查、`yarn build:dev` 和 `yarn build` 通过。`yarn run check` 已执行，但全仓 Prettier 仍被 24 个本轮未修改的既有文件拦截；未为本功能批量格式化无关文件。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.7.0` 开发构建。勾选自定义模式后正确显示钥匙来源、钥匙材料购买方式和货物出售方式；切换购买钥匙后，购买方式文案同步变为“钥匙购买方式”，左侧分组同步变为“购买钥匙”。
- 自定义计算：奇幻洞穴 T0、30 分钟下，制作钥匙 + 左一购入 + 左一卖出的自定义门票/开箱钥匙单位成本为 `644.53K / 2.67M`，单个普通宝箱、每日和每车期望收益为 `638.32K / 39.68M / 826.62K`；购买钥匙 + 右一购入 + 右一卖出时对应为 `620K / 2.65M` 和 `566.18K / 35.19M / 733.2K`。两组结果均随参数更新，左侧两列始终保留所选来源的保守/乐观区间。
- 交互稳定性：首次真实页面验证发现动态插入模板会触发 `uhtml InvalidNodeTypeError`，已改为稳定 DOM 节点配合 `hidden` 切换并增加回归断言。最终反复启用、关闭自定义模式及切换制作/购买来源均无业务错误；控制台只剩 Facebook Pixel 被浏览器拦截，与 MST 无关。
- 响应式：1280px 下弹窗宽 `1088px`，操作区四列，弹窗横向溢出为 `0`。390px 下自定义模式的 10 个可见控件自动排为两列，无控件重叠，页面、弹窗和操作区横向溢出均为 `0`；结果表仅在自身容器保留 `174px` 横向滚动。

## 2.6.6 验收记录（2026-08-02）

- 自动检查：地下城专项 30 项测试、全量 111 项测试、全量 ESLint、`yarn build:dev`、`yarn build` 通过，本轮 9 个涉及文件的 Prettier 检查通过。
- 计算回归：自动测试确认普通/精炼宝箱收益均除以对应每日宝箱数量形成单箱值，每日期望收益仍按未舍入的单箱收益乘数量汇总，门票与钥匙成本不重复扣除。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.6.6` 开发构建。奇幻洞穴 T2、30 分钟下，每日普通/精炼宝箱均为 `62.16`；制作钥匙的单个普通/精炼宝箱保守与乐观收益分别为 `531.97K / 746.01K`、`364.88K / 527.87K`，购买钥匙分别为 `453.49K / 703.08K`、`281.88K / 473.75K`；每日期望收益保持 `55.75M / 79.18M / 45.71M / 73.15M`。
- 布局与错误检查：1280px 视口下弹窗横向溢出为 `0`；控制台只有 Facebook Pixel 被浏览器拦截，与 MST 功能无关，未出现 MST 业务错误。

## 2.6.5 验收记录（2026-08-02）

- 自动检查：地下城专项 30 项测试、全量 111 项测试、全量 ESLint、`yarn build:dev`、`yarn build` 通过，本轮 9 个涉及文件的 Prettier 检查通过。
- 计算回归：普通宝箱分别扣除对应来源的门票与普通宝箱开箱钥匙成本，精炼宝箱分别扣除对应来源的开箱钥匙成本；自动测试确认每日收益等于两类宝箱收益之和再减药品/饮料成本，不重复扣除钥匙成本。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.6.5` 开发构建。奇幻洞穴 T2、30 分钟下，制作钥匙的普通/精炼宝箱保守与乐观收益分别为 `33.07M / 46.37M`、`22.68M / 32.81M`，购买钥匙分别为 `28.19M / 43.7M`、`17.52M / 29.45M`；每日期望收益四列均等于对应两类宝箱收益之和。
- 布局与错误检查：1280px 视口下弹窗横向溢出为 `0`；控制台只有 Facebook Pixel 被浏览器拦截，与 MST 功能无关，未出现 MST 业务错误。

## 2.6.4 验收记录（2026-08-02）

- 自动检查：地下城专项 29 项测试、全量 ESLint、`yarn build:dev`、`yarn build` 通过，本轮 7 个涉及文件的 Prettier 检查通过。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.6.4` 开发构建；首次打开默认选择官方排序第一位的“奇幻洞穴”、`T0` 和 `30` 分钟，每日轮次为 `48`。
- 布局与错误检查：1280px 视口下弹窗横向溢出为 `0`；控制台只有 Facebook Pixel 被浏览器拦截，与 MST 功能无关，未出现 MST 业务错误。

## 2.6.3 验收记录（2026-08-02）

- 自动检查：全量 ESLint、110 项 `yarn test`、`yarn build:dev`、`yarn build` 通过，本轮 7 个涉及文件的 Prettier 检查通过。
- 最高等级规则：角色物品数据中所有实际持有的暴饮之囊统一比较强化等级，不再优先当前装备，仅排除市场挂单；自动测试确认同时存在 `+5`、`+12`、`+15` 时选择 `+15`，只有市场挂单时不勾选。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.6.3` 开发构建；工匠茶默认勾选，暴饮自动勾选并读取当前最高等级 `+5`，弹窗横向溢出为 `0`。
- 错误检查：控制台只有 Facebook Pixel 被浏览器拦截，与 MST 功能无关；未出现 MST 业务错误。

## 2.6.2 验收记录（2026-08-02）

- 自动检查：全量 ESLint、110 项 `yarn test`、`yarn build:dev`、`yarn build` 通过，本轮 8 个涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 的 lint 通过，仍停在仓库既有的 24 个非本轮 Prettier 文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.6.2` 开发构建的地下城收益计算器。
- 默认 Buff：工匠茶自动勾选；角色当前拥有并装备 `+5` 暴饮之囊，页面自动勾选暴饮并选中 `+5`。自动测试另覆盖仅背包时取最高强化等级、只有市场挂单时不勾选的分支。
- 摘要布局：1280px 下 T0 的每日轮次、门票、普通宝箱和市场数据时间为同一行四格；T1 加入精炼宝箱后为同一行五格，所有格子顶边一致，页面、弹窗和摘要横向溢出均为 `0`。
- 收益样式：普通宝箱税后收益、精炼宝箱税后收益、每日期望收益和每车期望收益均使用 `mst-dungeon-row-revenue`，实测文字颜色和背景一致。
- 移动端：390px 下 T1 五个摘要格保持同一行，各格内容溢出均为 `0`；页面、弹窗、操作区和摘要横向溢出均为 `0`，六列表格只在自身容器横向滚动。
- 错误检查：控制台只有 Facebook Pixel 被浏览器拦截，与 MST 功能无关；未出现 MST 业务错误。

## 2.6.1 验收记录（2026-08-02）

- 自动检查：全量 ESLint、109 项 `yarn test`、`yarn build:dev`、`yarn build` 通过，本轮 9 个涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 的 lint 通过，仍停在仓库既有的 24 个非本轮 Prettier 文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.6.1` 开发构建的地下城收益计算器。
- 分区与语言：中文结果上半区显示“材料成本”、下半区显示“预期产出”；英文对应显示 `Material Costs / Expected Output`，价格方向只使用当前语言，不存在中英文混排。
- 暴饮交互：暴饮之囊使用无文本方形复选控件，1280px 下外框为 `30 × 30px`；默认勾选且等级为 `+5`，未启用工匠茶时等级仍可选择，取消勾选后等级立即禁用，再次勾选后恢复。
- 成本与摘要：T1、14 分钟、每日药品/饮料成本 `10M` 时，页面显示每日轮次 `102.86`、门票/普通宝箱 `133.2`、精炼宝箱 `43.96`，成本行出现且四列均为 `10M`。
- 响应式：1280px 操作区为四列且 T1 摘要为单行四格；390px 操作区为两列且摘要仍为单行四格。页面、弹窗、操作区和摘要横向溢出均为 `0`，六列表格只在自身容器横向滚动。
- 错误检查：全部交互和中英文切换后无 MST 业务错误；控制台仅有 Facebook Pixel 被浏览器拦截，与本功能无关。

## 2.6.0 验收记录（2026-08-02）

- 自动检查：全量 ESLint、109 项 `yarn test`、`yarn build:dev`、`yarn build` 通过，本轮 10 个涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 的 lint 通过，仍停在仓库既有的 24 个非本轮 Prettier 文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.6.0` 开发构建的地下城收益计算器。
- 双成本表：中文双层表头为“制作钥匙 / 购买钥匙”，每组均显示“右一卖出 / 左一购入”和“左一卖出 / 右一购入”；英文对应只显示 `Craft Keys / Buy Keys` 与英文价格方向，不再中英文混排，也不存在成本方式输入。
- 每日消耗成本：海盗 T0、14 分钟输入 `10M` 后，四列每日总成本分别从 `809.86M / 784.55M / 739.26M / 719.28M` 增加到 `819.86M / 794.55M / 749.26M / 729.28M`，每日及每车期望收益同步扣除对应分摊成本；清空输入时该成本行动态移除。
- 工匠茶与暴饮之囊：启用工匠茶后暴饮等级可选；取消暴饮复选框后等级立即禁用，制作钥匙成本发生变化，购买钥匙的每日总成本保持 `749.26M / 729.28M` 不变。
- 产出与摘要：T0 只显示普通宝箱税后收益，三个摘要格保持单行等宽且无空位；T1 增加 `43.96` 个精炼宝箱及精炼宝箱税后收益行，四个摘要格保持单行等宽。
- 响应式：1280px 下操作区首行 4 列、摘要 1 行，页面/弹窗/操作区/结果表横向溢出均为 `0`；390px 下操作区自动为 2 列、四个摘要仍为 1 行，页面/弹窗/操作区无横向溢出，六列表格只在自身容器内横向滚动。
- 对比度与错误检查：两层表头使用不同背景色；全部交互后无 MST 业务错误，控制台仅有 Kongregate API 连接关闭和 Facebook Pixel 被拦截，均与本功能无关。

## 2.5.16 验收记录（2026-08-01）

- 分区文案：制作材料模式显示上方“材料成本”、下方“预期产出”；成品市场模式显示上方“成品成本”、下方“预期产出”，旧“宝箱期望产出”文案已移除。
- 中英文：英文对应显示 `Material Costs / Expected Output` 和 `Finished Item Costs / Expected Output`，切换语言后弹窗即时更新，随后已恢复中文。
- 自动检查：全量 ESLint、108 项 `yarn test`、`yarn build:dev`、`yarn build` 通过，本轮涉及文件的 Prettier 检查通过。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，确认两段标题顺序正确、弹窗横向溢出为 `0`，成本方式和语言切换均无 MST 业务错误。
- 错误检查：控制台仅有 Kongregate API 连接关闭及 Facebook Pixel 被浏览器拦截，均与本功能无关。

## 2.5.15 验收记录（2026-08-01）

- 展示调整：门票单位成本和开箱钥匙单位成本已移入材料/成品成本表；每车门票成本、每车宝箱钥匙成本、每车总成本和材料消耗格子均已移除，只保留单位与每日成本。
- 数字格式：海盗 14 分钟下，T0 显示每日轮次 `102.86`、门票和普通宝箱 `133.2`；T1 精炼宝箱为 `43.96`，T2 为 `133.2`。成品单位成本可显示为 `1M`、`4.4M`，确认数量和金额最多保留两位小数且不补尾随 `0`。
- 自动检查：全量 ESLint、108 项 `yarn test`、`yarn build:dev`、`yarn build` 通过，本轮涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 的 lint 通过，仍停在仓库既有的 24 个非本轮 Prettier 文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.5.15` 地下城收益计算器。
- 响应式：1280px、700px、390px 下操作区分别为 4、3、2 列，无控件重叠；页面、弹窗和操作区横向溢出均为 `0`，390px 结果表仅在自身容器保留预期的 `17px` 横向滚动。
- 成本交互：制作材料成本与成品市场价往返切换两轮无报错；成品模式下工匠茶和暴饮之囊禁用，返回制作模式后工匠茶可用，启用后暴饮之囊默认 `+5` 且单位与每日成本同步更新，页面始终不显示材料消耗格子。
- 错误检查：全部交互后没有 MST 业务错误；控制台仅有 Kongregate API 连接关闭及 Facebook Pixel 被浏览器拦截，均与本功能无关。

## 2.5.14 验收记录（2026-08-01）

- 规则落地：四个地下城统一使用每车 `1.295` 个普通宝箱；T0/T1/T2 精炼宝箱分别为 `0`、`1.295 × 0.33`、`1.295`。每日轮次、单车/每日宝箱和门票均保留两位小数，门票数量等于普通宝箱期望。
- 自动检查：本轮涉及文件的 ESLint 与 Prettier 检查通过；全量 105 项 `yarn test`、`yarn build:dev`、`yarn build` 通过。
- 完整检查：`yarn run check` 的 lint 通过，仍停在仓库既有的 24 个非本轮 Prettier 文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.5.14` 地下城收益计算器。
- 固定数量：海盗 14 分钟下，T0/T1/T2 的每日轮次均为 `102.86`，普通宝箱和门票均为 `133.20`；精炼宝箱依次为不展示、`43.96`、`133.20`。对应每车宝箱钥匙为 `1.30`、`1.73`、`2.60`。
- 成本交互：制作材料成本与成品市场价连续往返切换两次无报错；成品模式下工匠茶和暴饮之囊均禁用，返回制作模式后启用工匠茶，暴饮之囊保持默认 `+5` 且可编辑，材料消耗更新为 `88.90%`。
- 收益展示：T0 不存在精炼宝箱行；T1/T2 正常显示普通宝箱、精炼宝箱、每日期望收益和每车期望收益。门票与宝箱钥匙均显示每车和每日成本，页面不再显示多余的每日卖出收益汇总行。
- 确定性：关闭并重新打开后，默认海盗 T0 仍为每日轮次 `102.86`、门票/普通宝箱 `133.20`、每日期望收益 `-734.19K / 48.43M`、每车期望收益 `-7.14K / 470.82K`。
- 说明与响应式：帮助说明明确显示 `1.295`、T1 `× 0.33`、门票等于普通宝箱和固定 2% 市场税；1280px 无横向溢出，390px 操作区为两列且无控件重叠，页面、弹窗和工具栏横向溢出均为 `0`，结果表容器保留预期的 `17px` 横向滚动。
- 错误检查：全部交互后无 MST 业务错误；控制台唯一错误为浏览器拦截 Facebook Pixel `https://connect.facebook.net/en_US/fbevents.js`，与本功能无关。

## 2.5.13 验收记录（2026-08-01）

- 规则核对：官方战斗说明将 Combat Drop Rate 限定为常规物品掉落；官方地下城奖励界面只按 `rewardDropTable` 和难度计算概率。参考“实用工具”同样未把 Combat Drop Rate 应用于地下城宝箱，确认旧实现重复放大了奖励概率。
- 自动检查：`yarn lint`、106 项 `yarn test`、`yarn build:dev`、`yarn build` 通过；本轮涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 仍停在仓库既有的 24 个非本轮 Prettier 文件，本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6` 并从真实工具箱打开地下城收益计算器。
- 固定概率：页面不存在“战斗掉落率 / Combat Drop Rate”输入；海盗 T1、14 分钟、5 人、手动 Combat Drop Quantity `0%` 时，每日实际通关和普通宝箱均为 `102`，精炼宝箱为 `33.66 = 102 × 0.33`，与官方 T1 固定概率一致。
- 交互与说明：关闭“读取角色 Buff”后唯一的 Combat Drop Quantity 输入解除禁用并可填写；成本方式往返切换正常；帮助说明明确显示地下城宝箱奖励概率由官方难度固定。
- 响应式：1280px 操作区保持三排，390px 自动改为两列；页面、弹窗和操作区横向溢出均为 `0`，390px 结果表仅在自身容器保留预期的 `17px` 横向滚动范围。
- 错误检查：全部交互后控制台唯一错误为浏览器拦截 Facebook Pixel `https://connect.facebook.net/en_US/fbevents.js`，与 MST 无关。

## 2.5.12 验收记录（2026-08-01）

- 自动检查：`yarn lint`、106 项 `yarn test`、`yarn build:dev`、`yarn build` 通过；本轮 8 个涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 仍停在仓库既有的 24 个非本轮 Prettier 文件，本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开显示新版单日期望结果的地下城收益计算器。
- 单日结果：结果表只保留一行“每日期望净利润”，页面不存在 3、7、14 天期望文案；默认海盗 T0、14 分钟、5 人和角色 Buff 下，每日实际通关及门票均为 `102`，普通宝箱期望为 `132.09`。
- 算式：期望卖出总价为 `798.33M / 821.9M`，每日总成本为 `768.2M / 743.71M`，每日期望净利润为 `30.13M / 78.2M`，收入减成本与页面结果在显示精度内一致。
- 交互与响应式：制作材料成本与成品市场价往返切换正常且未增加错误；1280px 操作区为三排四列且无横向溢出，390px 为两列且无控件重叠，结果表仅在自身容器保留预期的 `17px` 横向滚动范围。
- 错误检查：全部交互后控制台唯一错误为浏览器拦截 Facebook Pixel `https://connect.facebook.net/en_US/fbevents.js`，与 MST 无关。

## 2.5.11 验收记录（2026-08-01）

- 算法核对：宝箱期望数量使用实际通关次数、官方掉率、数量区间平均值和个人产量倍率；开箱按 `openableLootDropMap` 递归展开物品期望、合并重复物品并累计期望钥匙需求，门票不乘任何产量倍率。
- 自动检查：`yarn lint`、106 项 `yarn test`、`yarn build:dev`、`yarn build` 通过；本轮 9 个涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 仍停在仓库既有的 24 个非本轮 Prettier 文件，本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开显示 `2.5.11` 新文案的地下城收益计算器。
- 确定性与算式：默认海盗 T0、14 分钟、5 人时，日均实际通关、门票和普通宝箱均为 `102.67`；期望卖出总价为 `620.5M / 638.82M`，总成本为 `624.21M / 604.71M`，3 天期望单日净利润为 `-3.71M / 34.12M`，收入减成本与页面结果在显示精度内一致。关闭并重新打开后，上述所有值逐项相同。
- Buff 与门票：取消“读取角色 Buff”后，两项输入解除禁用；填写 Combat Drop Quantity `35%`、Combat Drop Rate `12%` 后，T0 普通宝箱从 `102.67` 增至 `138.6`，切换 T1 后精炼宝箱为 `51.23`，门票始终保持 `102.67`。
- 交互：制作材料成本与成品市场价连续往返切换两次，分组和结果正常更新，未增加控制台错误；列表只显示 3、7、14 天三组期望单日净利润。
- 响应式：1280px 操作区严格为三排四列，390px 为两列；两种视口均无控件重叠、页面或弹窗横向溢出，390px 结果表仅在自身容器保留预期的 `17px` 横向滚动范围。
- 错误检查：全部交互后控制台唯一错误为浏览器拦截 Facebook Pixel `https://connect.facebook.net/en_US/fbevents.js`，与 MST 无关。
- 未覆盖：真实市场数据未自然出现完全缺价物品；缺价按 `0` 估值并聚合提示的分支由自动测试覆盖，不记作真实缺价场景已通过。

## 2.5.10 验收记录（2026-08-01）

- 参考核对：重新检查“康康运气”和“实用工具”的 `openableLootDropMap` 处理逻辑，并与本地官方 `init_client_data.json`、MWICombatSimulator 的小数 `randomInt` 实现交叉核对；确认地下城奖励先生成整数宝箱，再逐箱读取官方掉落率及数量区间，重复物品合并，嵌套宝箱继续开启。
- 自动检查：`yarn lint`、106 项 `yarn test`、`yarn build:dev`、`yarn build` 通过；本轮 9 个涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 仍停在仓库既有的 24 个非本轮 Prettier 文件，本次未整仓格式化，避免产生无关改动。
- Playwright MCP 真实页面验证：仅使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6`，从真实工具箱打开 `2.5.10` 开发构建的地下城收益计算器。
- 模拟与算式：默认海盗 T0、14 分钟、5 人、角色 `29.5%` 战斗掉落数量时，本次 3 天模拟的日均宝箱为 `134`；保守侧 `790.43M - 778.37M = 12.06M`，乐观侧 `813.86M - 753.54M = 60.32M`，实际模拟掉落卖出总价减门票和开箱钥匙成本后与页面净利润完全一致。
- 随机状态：同一弹窗内切换成本方式后，普通宝箱数量及卖出总价保持为 `118 / 670.07M / 690.55M`；关闭并重新打开后默认模拟变为 `134 / 790.43M / 813.86M`，确认普通重绘不重抽、重新打开才开始新一轮模拟。
- 交互：取消“读取角色 Buff”后，战斗掉落数量和战斗掉落率解除禁用并成功填写 `10`、`20`；制作材料成本与成品市场价往返切换正常，结果分组同步变化且未增加控制台错误；关闭市场税后，普通宝箱两侧卖出总价从 `799.91M / 823.99M` 变为 `817.92M / 842.63M`，确认两侧收入均应用税费。
- 周期结果：页面只显示 3、7、14 天三组模拟单日净利润，每个周期使用独立随机序列；同一次弹窗内参数重绘不会改变已经抽取的模拟结果。
- 响应式：1280px 操作区为四列，390px 为两列；两种视口均无控件重叠、弹窗溢出或页面级横向溢出，390px 结果表仅在自身容器保留预期的 `17px` 横向滚动范围。
- 错误检查：全部交互后控制台唯一错误为浏览器拦截 Facebook Pixel `https://connect.facebook.net/en_US/fbevents.js`，与 MST 无关。
- 未覆盖：真实市场数据未自然出现完全缺价物品；缺价按 `0` 估值并聚合提示的分支由自动测试覆盖，不记作真实缺价场景已通过。

## 2.5.9 验收记录（2026-08-01）

- 参考核对：分析“康康运气”和“实用工具”两份 `script.source.js`，确认单项公式为 `dropRate × (minCount + maxCount) / 2 × itemPrice`，重复掉落合并，嵌套宝箱和代币迭代估值。
- 自动检查：`yarn lint`、105 项 `yarn test`、`yarn build:dev`、`yarn build` 通过；本轮 10 个涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 停在仓库既有的 Prettier 基线问题，列出 24 个非本轮文件；未整仓格式化，避免无关改动。
- Playwright MCP 真实页面验证：使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，登录角色 `dadada6` 并从真实工具箱打开地下城收益计算器。
- 成本与产出：海盗 T0、14 分钟、5 人和角色 `29.5%` 掉落数量时，页面显示每日 `102.86` 次通关、`133.2` 个普通宝箱；结果分为“材料成本”和“宝箱预期产出”，每日总成本 `774.66M / 749.96M`，宝箱预期产出 `808.66M / 843.66M`，净利润 `34M / 93.7M`，加减关系一致。
- 随机产出交互：取消读取角色 Buff 后填写掉落数量 `35%`、掉落率 `12%`，T0 普通宝箱变为 `138.86` 个；切换 T1 后精炼宝箱为 `51.32` 个，对应预期产出 `240.47M / 250.19M`，证明收益随宝箱概率与数量重新计算。
- 交互与语言：制作材料成本和成品市场价往返切换正常，分组标题同步变为“材料成本/成品成本”；英文显示 `Material Costs`、`Expected Chest Contents`、`Expected Normal Chest Value` 及官方 `Combat Drop Quantity/Rate`，随后已恢复中文和默认 Buff。
- 响应式：1280px 操作区为四列，页面、操作区和结果表横向溢出均为 `0`；390px 操作区为两列且页面无横向溢出，只有结果表容器保留预期的 `17px` 横向滚动范围。
- 错误检查：全部交互后控制台唯一错误为浏览器拦截 Facebook Pixel `fbevents.js`，与 MST 无关。
- 未覆盖：真实市场数据未自然出现单侧 `-1` 或完全缺价；该分支由自动测试覆盖，不记作真实缺价场景已通过。

## 2.5.8 验收记录（2026-08-01）

- 自动检查：`yarn lint`、102 项 `yarn test`、`yarn build:dev`、`yarn build` 通过；本轮涉及文件的 Prettier 检查通过。
- 完整检查：`yarn run check` 停在仓库既有的 Prettier 基线问题；清理临时验证文件后，`yarn format:check` 列出 25 个既有文件，本次未整仓格式化。
- Playwright MCP 真实页面验证：使用 `mcp__playwright__*` 打开 `https://www.milkywayidle.com/game?characterId=427012`，成功加载角色 `dadada6`，从真实游戏顶部工具箱打开地下城收益计算器。
- 默认与结果：队伍人数为 5、暴饮之囊为 `+5`，无统计周期控件；列表显示 3/7/14 天三组模拟单日净利润，门票、钥匙、材料消耗和市场数据时间固定为四格。
- 交互：取消读取角色 Buff 后，两项输入解除禁用并成功填写 `35`、`12`，三组单日净利润随之从 `34.68M / 93.39M` 更新为 `41.22M / 102.34M`；成本方式连续往返切换四次，材料消耗在 `100%` 与 `-` 间正确变化。
- 工匠茶：启用后暴饮之囊强化等级保持默认 `+5` 且可编辑，真实游戏数据计算出的材料消耗为 `88.9%`。
- 中英文：真实弹窗切换英文后显示官方 `Combat Drop Quantity`、`Combat Drop Rate` 和 `Guzzling Pouch Enhancement Level`，切回中文后状态正常。
- 响应式：1280px 为四列，390px 为两列；真实页面、弹窗、操作区和单位成本区横向溢出均为 `0`，结果表只在自身容器内横向滚动。
- 错误检查：全部交互后页面业务错误数组为空；控制台唯一错误为浏览器拦截 Facebook Pixel `fbevents.js`，与 MST 无关。
- 未覆盖：真实市场数据完整，未自然触发缺价提示；该分支由自动测试和组件级动态显示/隐藏测试覆盖，不记作真实缺价场景已通过。

## 2.5.7 验收记录（2026-08-01）

- 自动检查：`yarn test` 共 101 项全部通过，`yarn lint`、`yarn build:dev`、`yarn build` 通过。
- 完整检查：`yarn run check` 停在仓库既有的 Prettier 基线问题，共 27 个文件；本次未整仓格式化，避免产生无关改动。
- Playwright MCP 补充验证：使用 `mcp__playwright__*` 打开临时地址 `http://127.0.0.1:4174/_browser-dungeon-preview.html`；页面引用实际 `integrated.css` 和同一操作区 DOM，验收后已删除。
- 1280px 视口：12 个控件严格分成三排，纵坐标分别为 `70 / 125 / 180`，每排顺序与需求一致，横向溢出为 `0`。
- 390px 视口：操作区自动改为两列，页面和工具栏横向溢出均为 `0`。
- 交互：取消“读取角色 Buff”后，战斗掉落数量和战斗掉落率两个输入框同时解除禁用。
- 控制台：没有地下城业务脚本错误；记录到 1 个临时预览页 `favicon.ico` 404 和 2 个 Playwright 扩展资源错误，均来自验证环境。
- 未覆盖：Playwright MCP 浏览器没有登录游戏，未能打开登录后的真实工具箱弹窗；本地预览结果不得记为实际目标页面验证通过。

## 版本状态

- EDS 历史线保留 `1.x`，融合基础版本为 `1.2.7`。
- MST 综合工具箱从 `2.0.0` 起步。
- 当前正式版本为 `2.7.22`，对应输入与视口兼容修复。
- 版本遵循 SemVer：不兼容变更提升 major，向后兼容的新功能提升 minor，修复和兼容调整提升 patch。
- 功能或用户可见行为变化必须提升版本号并更新双语更新日志；纯文档、注释、格式和测试调整可以不提升。
- 开发包使用自动生成的 `-dev.<timestamp>` 后缀。

## 交付要求

- 用户可见功能变化更新 `docs/usage.md`。
- 长期业务规则变化更新 `docs/业务功能要求.md`。
- 算法、数据来源和限制更新 `docs/analysis/` 下对应文档。
- 清理任务产生的临时文件，不删除历史备份和专项参考资料。
- 每次交付前优先执行 `yarn run check`；无法执行时说明原因。
- 较大改动除自动检查外，必须通过 Playwright MCP 服务打开实际页面完成浏览器验证；极小且低风险的改动可以不验证，但要说明。
- Playwright MCP 只认可 `mcp__playwright__*` 工具；Browser plugin / Browser skill 内置 Playwright、Node REPL 浏览器控制和 `playwright-cli` 均不得替代。
- 验证记录必须区分实际页面与本地预览，并写明 URL/页面类型、视口、关键交互、控制台结果及未覆盖风险。
